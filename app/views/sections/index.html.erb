<h1>Listing sections</h1>

<table>
  <tr>
    <th>Course</th>
    <th>Section number</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sections.each do |section| %>
  <tr>
    <td><%= section.course_id %></td>
    <td><%= section.section_id %></td>
    <td><%= link_to 'Show', 'course_section_path(section)' %></td>
  </tr>
<% end %>
</table>

<br />
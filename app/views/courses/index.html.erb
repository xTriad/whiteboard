<h1>Courses</h1>

<table>
<% @courses.each do |course| %>
  <tr>
    <td><%= course.number %></td>
    <td><%= course.name %></td>
    <td><%= link_to 'Show', course %></td>
  </tr>
<% end %>
</table>

<br />
